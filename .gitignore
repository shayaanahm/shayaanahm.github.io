_previews/

# Impeccable design-detector per-session cache — pure machine state.
# NOT .impeccable/ wholesale: config.json holds the suppression decisions and
# the reasons for them, which is real project knowledge and should be shared.
.impeccable/hook.cache.json

# Throwaway single-section screenshot benches. See CLAUDE.md -> Verifying
# design changes: these must never be committed.
_bench.html
