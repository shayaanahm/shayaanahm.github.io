# Old site backup

Snapshot of the portfolio as it was **before the 2026-07 brand-evolution redesign**
(the version live until July 2026). Extracted from git tag
`backup-before-scrollworld-redesign` (commit `a6737e2`); this README replaces the
snapshot's original two-line repo README.

Kept as a browsable backup — the pages in here are self-contained with their own
`style.css`/`script.js` and are not linked from the current site.
